# 前端架構概述

本文檔概述虛擬太空人互動藝術裝置專案的前端架構，包含核心技術、目錄結構、狀態管理、主要模組功能等關鍵資訊。

## 核心技術

虛擬太空人前端採用現代化 Web 技術棧，主要包含：

- **React 19**: 使用最新版本的 React 框架構建使用者介面
- **TypeScript**: 強型別 JavaScript 超集，提供更可靠的開發體驗
- **Vite**: 現代化前端打包與開發工具，提供快速的開發與建置體驗
- **Three.js**: 3D 渲染引擎，用於虛擬太空人模型渲染
- **@react-three/fiber**: Three.js 的 React 整合層
- **@react-three/drei**: Three.js 的 React 輔助工具庫
- **Zustand**: 輕量級狀態管理庫，用於管理應用狀態
- **Web Audio API**: 處理音訊分析與反應式效果
- **TailwindCSS**: 原子化 CSS 框架，用於快速構建介面
- **WebSocket**: 與後端進行即時雙向通訊
- **Framer Motion**: 動畫庫，提供流暢的 UI 動畫效果
- **React Hot Toast**: 通知提示系統
- **React Draggable**: 可拖拽元件支援
- **React Resizable**: 可調整大小的元件支援
- **React Icons**: 圖標庫
- **UUID**: 唯一識別碼生成

## 目錄結構樹

```plaintext
prototype/frontend/
├── public/                    # 靜態資源
│   ├── animations/            # 3D 模型動畫檔案 (.glb)
│   ├── audio/                 # 音效與背景音樂
│   │   ├── BGM/               # 背景音樂
│   │   └── effects/           # 音效
│   ├── models/                # 3D 模型檔案 (.glb)
│   └── vite.svg              # Vite 圖標
├── src/                       # 源碼目錄
│   ├── assets/                # 靜態資源
│   ├── camera/                # 攝影機管理系統
│   │   ├── CameraManager.ts   # 攝影機管理器
│   │   ├── index.ts           # 攝影機模組入口
│   │   └── useCameraManager.ts # 攝影機管理 Hook
│   ├── components/            # React 元件
│   │   ├── layout/            # 佈局相關元件
│   │   │   ├── AppUI.tsx      # 主應用界面佈局
│   │   │   └── SceneContainer.tsx # 場景容器佈局
│   │   ├── AudioMixer.tsx     # 音訊混音控制台
│   │   ├── AudioReactiveBackground.tsx # 音訊反應背景
│   │   ├── BackgroundSoundSystem.tsx # 背景音效系統
│   │   ├── BodyModel.tsx      # 身體模型元件
│   │   ├── DanceGroup.tsx     # 舞蹈群組元件
│   │   ├── DirectorLogPanel.tsx # 導演日誌面板
│   │   ├── DirectorMonitorHUD.tsx # 導演監控 HUD
│   │   ├── DynamicAudioBackgrounds.tsx # 動態音訊背景
│   │   ├── EffectBackground.tsx # 特效背景
│   │   ├── ErrorBoundary.tsx  # 錯誤邊界元件
│   │   ├── FloatingChatWindow.tsx # 浮動聊天視窗
│   │   ├── HeadModel.tsx      # 頭部模型元件
│   │   ├── ImageOverlay.tsx   # 圖片覆蓋層
│   │   ├── ModelAnalyzerTool.tsx # 模型分析工具
│   │   ├── ModelDebugger.tsx  # 模型調試器
│   │   ├── ModelViewer.tsx    # 模型檢視器
│   │   ├── MorphTargetBar.tsx # 表情控制滑動條
│   │   ├── MorphTargetControls.tsx # 表情控制面板
│   │   ├── MusicBackground.tsx # 音樂背景元件
│   │   ├── P5SpaceEffect.tsx  # P5.js 風格太空特效
│   │   ├── RoomControlPanel.tsx # 房間控制面板
│   │   ├── RoomScene.tsx      # 房間場景
│   │   ├── SceneContainer.tsx # 場景容器
│   │   ├── SceneManager.tsx   # 場景管理器
│   │   ├── SettingsPanel.tsx  # 設定面板
│   │   ├── SpeechBackground.tsx # 語音背景元件
│   │   ├── Toast.tsx          # 通知提示元件
│   │   └── VideoPlayer.tsx    # 影片播放器
│   ├── config/                # 配置檔案
│   │   ├── animationConfig.ts # 動畫配置
│   │   ├── emotionMappings.ts # 情緒映射配置
│   │   ├── modelConfig.ts     # 模型配置
│   │   ├── resources.ts       # 資源配置
│   │   └── sceneConfig.ts     # 場景配置
│   ├── director/              # 導演系統
│   │   └── bus.ts             # 事件匯流排
│   ├── hooks/                 # 自定義 React Hooks
│   │   ├── useEmotionalSpeaking.ts # 情緒化語音 Hook
│   │   └── usePerformanceMetrics.ts # 效能監控 Hook
│   ├── lighting/              # 燈光系統
│   │   └── lightingRig.ts     # 燈光設備
│   ├── services/              # 服務層
│   │   ├── AudioService.ts    # 音訊處理服務
│   │   ├── BodyService.ts     # 身體模型服務
│   │   ├── ChatService.ts     # 聊天服務
│   │   ├── HeadService.ts     # 頭部模型服務
│   │   ├── WebSocketService.ts # WebSocket 服務
│   │   ├── api.ts             # API 請求服務
│   │   ├── audioPlayer.ts     # 音訊播放器
│   │   └── index.ts           # 服務模組入口
│   ├── store/                 # Zustand 狀態管理
│   │   ├── slices/            # 狀態切片
│   │   │   ├── appSlice.ts    # 應用狀態切片
│   │   │   ├── audioSettingsSlice.ts # 音訊設定切片
│   │   │   ├── audioSlice.ts  # 音訊狀態切片
│   │   │   ├── backgroundAudioSlice.ts # 背景音訊切片
│   │   │   ├── bodySlice.ts   # 身體模型切片
│   │   │   ├── chatSlice.ts   # 聊天狀態切片
│   │   │   ├── headSlice.ts   # 頭部模型切片
│   │   │   ├── mediaSlice.ts  # 媒體狀態切片
│   │   │   ├── roomSlice.ts   # 房間狀態切片
│   │   │   ├── runtimeSlice.ts # 運行時狀態切片
│   │   │   ├── speechTextSlice.ts # 語音文字切片
│   │   │   └── webSocketSlice.ts # WebSocket 狀態切片
│   │   └── index.ts           # 狀態管理入口
│   ├── styles/                # 全局樣式
│   ├── types/                 # TypeScript 型別定義
│   │   └── animation.ts       # 動畫型別定義
│   ├── utils/                 # 工具函數
│   │   ├── LogManager.ts      # 日誌管理工具
│   │   ├── ModelAnalyzer.ts   # 3D模型分析工具
│   │   └── animationUtils.ts  # 動畫處理工具函數
│   ├── App.css                # App 樣式
│   ├── App.tsx                # 主應用元件
│   ├── index.css              # 全局樣式
│   ├── main.tsx               # 應用入口
│   ├── README.md              # 前端說明文件
│   └── vite-env.d.ts          # Vite 環境型別定義
├── eslint.config.js           # ESLint 配置
├── package.json               # 專案依賴與腳本
├── postcss.config.js          # PostCSS 配置
├── tailwind.config.js         # TailwindCSS 配置
├── tsconfig.app.json          # TypeScript 應用配置
├── tsconfig.node.json         # Node TypeScript 配置
└── vite.config.ts             # Vite 配置
```

## 狀態管理架構

前端採用 Zustand 進行狀態管理，將不同功能領域的狀態拆分為多個狀態切片（slices）：

### 主要狀態切片

1. **appSlice**: 應用全局狀態，管理整體應用設置與界面狀態
2. **audioSlice**: 音訊處理相關狀態，管理音量、音效播放等
3. **audioSettingsSlice**: 音訊設定狀態，調整 TTS/BGM 音量等參數
4. **backgroundAudioSlice**: 音訊驅動背景效果狀態
5. **bodySlice**: 身體模型相關狀態，包含模型位置、縮放、動畫等
6. **chatSlice**: 聊天功能相關狀態，管理對話歷史與當前對話
7. **headSlice**: 頭部模型相關狀態，包含表情控制、口型同步等
8. **mediaSlice**: 媒體控制相關狀態，管理錄音、播放狀態
9. **roomSlice**: 房間場景狀態，管理場景設定與環境參數
10. **runtimeSlice**: 運行時狀態，管理系統運行時資訊
11. **speechTextSlice**: 語音文字狀態，用於大螢幕文本顯示
12. **webSocketSlice**: WebSocket 連接相關狀態，管理連接狀態與消息

## 主要模組摘要

### 核心元件 (Components)

| 元件名稱 | 用途說明 |
|---------|---------|
| **3D 模型與動畫** |
| BodyModel.tsx | 處理虛擬太空人身體模型的渲染與動畫控制 |
| HeadModel.tsx | 處理虛擬太空人頭部模型的渲染與表情控制 |
| DanceGroup.tsx | 複製三個 BodyModel 形成同步舞群效果 |
| ModelViewer.tsx | 3D模型查看器，整合頭部與身體模型的顯示 |
| ModelDebugger.tsx | 模型調試界面，顯示模型狀態與控制項 |
| ModelAnalyzerTool.tsx | 3D模型分析工具，用於開發調試階段 |
| **表情與動畫控制** |
| MorphTargetBar.tsx | 表情控制滑動條元件，用於調整表情參數 |
| MorphTargetControls.tsx | 表情控制面板，組織多個表情控制項 |
| **音訊與背景效果** |
| AudioMixer.tsx | 混音控制台，調整各音軌音量 |
| AudioReactiveBackground.tsx | 根據音訊輸入動態變化的背景效果 |
| BackgroundSoundSystem.tsx | 管理背景音樂與音效播放，營造太空氛圍 |
| DynamicAudioBackgrounds.tsx | 整合並統一管理所有背景效果 |
| SpeechBackground.tsx | 依語音音量與播放時間顯示字幕，支援打字機同步 |
| MusicBackground.tsx | 漂浮的太空音樂播放器及粒子效果，支援瘋狂模式 |
| EffectBackground.tsx | 事件觸發的擴散粒子特效，持續時間較長 |
| P5SpaceEffect.tsx | P5.js 風格的粒子視覺，隨音樂強度變化 |
| **場景與環境** |
| SceneContainer.tsx | Three.js 場景容器，管理 3D 渲染環境 |
| SceneManager.tsx | 場景管理器，協調場景中的各種元素 |
| RoomScene.tsx | 房間場景元件，管理太空艙環境 |
| RoomControlPanel.tsx | 房間控制面板，調整場景參數 |
| **使用者介面** |
| FloatingChatWindow.tsx | 可拖動的聊天視窗界面，顯示對話歷史 |
| SettingsPanel.tsx | 應用設置面板，提供各種配置選項 |
| DirectorMonitorHUD.tsx | 導演監控 HUD，顯示系統狀態 |
| DirectorLogPanel.tsx | 導演日誌面板，記錄系統事件 |
| Toast.tsx | 通知提示元件，顯示系統消息 |
| ImageOverlay.tsx | 圖片覆蓋層元件 |
| VideoPlayer.tsx | 影片播放器元件 |
| **佈局與容器** |
| layout/AppUI.tsx | 應用主界面佈局，組織整體 UI 結構 |
| layout/SceneContainer.tsx | 場景容器佈局，管理 3D 場景的顯示區域 |
| **錯誤處理** |
| ErrorBoundary.tsx | React 錯誤邊界元件，捕獲渲染錯誤並優雅降級 |

### 服務層 (Services)

| 服務名稱 | 用途說明 |
|---------|---------|
| AudioService.ts | 音訊處理服務，包括錄音、播放與頻譜分析 |
| BodyService.ts | 身體模型管理服務，控制動畫與姿態 |
| ChatService.ts | 聊天服務，處理訊息並管理語音播放隊列與握手機制 |
| HeadService.ts | 頭部模型管理服務，控制表情與口型同步 |
| WebSocketService.ts | WebSocket 連接管理，含重連與高頻消息防抖 |
| api.ts | REST API 請求封裝，統一管理 HTTP 請求 |
| audioPlayer.ts | 音訊播放器實現，處理音效與語音播放 |
| index.ts | 服務模組入口，統一匯出所有服務 |

### 攝影機系統 (Camera)

| 檔案名稱 | 用途說明 |
|---------|---------|
| CameraManager.ts | 攝影機管理器，控制視角切換與動畫 |
| useCameraManager.ts | 攝影機管理 Hook，提供 React 整合 |
| index.ts | 攝影機模組入口，統一匯出 |

### 配置與工具

| 檔案名稱 | 用途說明 |
|---------|---------|
| **配置檔案** |
| config/animationConfig.ts | 動畫相關配置，定義可用動畫與參數 |
| config/emotionMappings.ts | 情緒到表情的映射配置，連接語義與視覺表現 |
| config/modelConfig.ts | 3D模型相關配置，設定模型路徑與參數 |
| config/resources.ts | 資源配置，管理靜態資源路徑 |
| config/sceneConfig.ts | 場景配置，設定場景參數與環境 |
| **自定義 Hooks** |
| hooks/useEmotionalSpeaking.ts | 情緒化語音表達 Hook，連接語音與表情變化 |
| hooks/usePerformanceMetrics.ts | 效能監控 Hook，追蹤系統效能指標 |
| **系統模組** |
| lighting/lightingRig.ts | 燈光設備配置，管理場景燈光 |
| director/bus.ts | 事件匯流排，處理系統內部事件通訊 |
| **工具函數** |
| utils/LogManager.ts | 日誌管理工具，統一處理日誌輸出 |
| utils/ModelAnalyzer.ts | 3D模型分析工具，提取模型結構與參數 |
| utils/animationUtils.ts | 動畫處理工具函數，輔助動畫控制 |
| **型別定義** |
| types/animation.ts | 動畫相關型別定義 |

## 開發與建置指令

專案提供多種 npm 指令用於開發、測試與建置：

```bash
# 開發模式啟動，支援熱更新
npm run dev

# 建置生產版本（包含動畫同步）
npm run build

# 預覽生產版本
npm run preview

# 代碼檢查
npm run lint

# 自動修復代碼問題
npm run lint:fix

# 格式化代碼
npm run format

# 同步動畫資源（從共享目錄同步到專案）
npm run sync-animations

# 執行測試
npm run test

# 啟動開發伺服器（備用）
npm run start

# 彈出配置（不建議使用）
npm run eject
```

## 技術特色

### 3D 渲染與動畫
- 使用 Three.js 與 React Three Fiber 實現高品質 3D 渲染
- 支援骨骼動畫與 Morph Target 表情控制
- 整合多個模型實例形成舞蹈群組效果
- 動態攝影機控制與預設位置管理

### 音訊驅動視覺效果
- Web Audio API 實現即時音訊分析
- 多層次背景效果系統，包含語音反應、音樂粒子、特效觸發
- P5.js 風格的程序化視覺效果
- 音訊混音控制台支援多軌音訊管理

### 即時互動系統
- WebSocket 實現前後端即時通訊
- 語音播放握手機制確保音訊順序
- 情緒化表情與動作同步
- 支援多種訊息類型與狀態同步

### 模組化架構
- Zustand 狀態管理提供清晰的資料流
- 服務層封裝業務邏輯
- 元件化設計支援功能擴展
- TypeScript 提供型別安全

### 開發者工具
- 導演監控系統提供即時狀態檢視
- 模型分析與調試工具
- 效能監控與日誌系統
- 錯誤邊界保護應用穩定性

### 響應式設計
- TailwindCSS 原子化樣式
- 支援拖拽與調整大小的元件
- Framer Motion 流暢動畫效果
- 適配不同螢幕尺寸

## 專案規模統計

- **總檔案數**: 70 個 TypeScript/JavaScript 檔案
- **元件數量**: 25+ 個 React 元件
- **狀態切片**: 12 個 Zustand 狀態切片
- **服務模組**: 7 個核心服務
- **配置檔案**: 5 個配置模組
- **工具函數**: 3 個工具模組

